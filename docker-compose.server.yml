# API only. SQLite in a named volume at /data/manitor.db. Dashboard run separately (or another compose).
services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    volumes:
      - server_data:/data
    environment:
      SERVER_CLIENT: ":5000"
    restart: unless-stopped

volumes:
  server_data:
