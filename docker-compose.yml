# Full stack: API + static dashboard. From your machine, the UI calls the API at localhost:5000.
services:
  server:
    build: ./server
    ports:
      - "9292:9293"
    environment:
      SERVER_CLIENT: ":9293"
      DB_HOST: ${POSTGRES_HOST:-postgres-kalif}
      DB_PORT: ${POSTGRES_PORT:-5432}
      DB_USER: ${POSTGRES_USER:-postgres}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      DB_NAME: ${POSTGRES_DB:-manitor}
      DB_SSL_MODE: ${POSTGRES_SSLMODE:-disable}
    networks:
      - devopsio
    restart: unless-stopped

  dashboard:
    build:
      context: ./dashboard
      args:
        VITE_SERVER_BASE_URL: ${VITE_SERVER_BASE_URL:-http://localhost:9293}
    ports:
      - "9293:80"
    depends_on:
      - server
    networks:
      - devopsio
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  devopsio:
    name: devopsio
    external: true
  